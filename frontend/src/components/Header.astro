---
// 极简版 Header
---

<header class="site-header">
  <div class="logo-container">
    <a href="/" class="brand-name">Tiny Crochet</a>
  </div>
  
  <nav class="nav-links">
    <a href="#" target="_blank">Instagram</a>
    <a href="mailto:your-email@example.com">Contact</a>
  </nav>
</header>

<style>
  .site-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.5rem 2rem;
    border-bottom: 1px solid var(--color-placeholder, #eaeaea);
    font-family: inherit;
  }

  .brand-name {
    font-size: 1.25rem;
    font-weight: 700;
    text-decoration: none;
    color: inherit;
    text-transform: uppercase; /* 大写字母更显高级 */
  }

  .nav-links {
    display: flex;
    gap: 1.5rem;
  }

  .nav-links a {
    text-decoration: none;
    color: inherit;
    font-size: 0.9rem;
    font-weight: 500;
    opacity: 0.6;
    transition: opacity 0.2s ease;
  }

  .nav-links a:hover {
    opacity: 1;
  }

  /* 手机端适配：稍微调小内边距 */
  @media (max-width: 768px) {
    .site-header {
      padding: 1rem;
    }
  }
</style>